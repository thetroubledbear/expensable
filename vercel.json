{
  "buildCommand": "npx turbo run build && cd apps/web && (npx payload migrate:create --name init || true) && (npx payload migrate || true)",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
